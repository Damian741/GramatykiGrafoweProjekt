from GraphFragment import GraphFragment
verticies_graph_fragment = {0 : GraphFragment(None, [0])}

def P1(id):
    graph_fragment = verticies_graph_fragment.get(id)
    new_fragment = GraphFragment z warstwy  niższej
    #dodanie do grafu w networkx i wszystkie przekształcenia itd...
    verticies_graph_fragment[środkowego wierzchołka] = new_fragment
